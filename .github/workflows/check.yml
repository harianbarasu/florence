name: check

on:
  pull_request:
  push:
    branches: [main, "codex/**"]

permissions:
  contents: read

jobs:
  check:
    runs-on: ubuntu-latest
    timeout-minutes: 20
    services:
      postgres:
        image: postgres:18-alpine
        env:
          POSTGRES_DB: florence_test
          POSTGRES_PASSWORD: florence
          POSTGRES_USER: postgres
        options: >-
          --health-cmd "pg_isready -U postgres -d florence_test"
          --health-interval 2s
          --health-timeout 3s
          --health-retries 20
        ports:
          - 5432:5432
    env:
      TEST_DATABASE_URL: postgres://postgres:florence@127.0.0.1:5432/florence_test
    steps:
      - uses: actions/checkout@v4
      - uses: pnpm/action-setup@v4
        with:
          version: 10.10.0
      - uses: actions/setup-node@v4
        with:
          node-version: 24.19.0
          cache: pnpm
      - run: pnpm install --frozen-lockfile
      - run: pnpm db:migrate
        env:
          FLORENCE_DATABASE_URL: ${{ env.TEST_DATABASE_URL }}
          FLORENCE_WEB_BASE_URL: http://localhost:3000
          FLORENCE_TOKEN_ENCRYPTION_KEY: AAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAA
          FLORENCE_DATA_ACTIVE_KEY_ID: ci-v1
          FLORENCE_DATA_KEYRING_JSON: '{"ci-v1":"BBBBBBBBBBBBBBBBBBBBBBBBBBBBBBBBBBBBBBBBBBB"}'
          FLORENCE_BLIND_INDEX_KEY: CCCCCCCCCCCCCCCCCCCCCCCCCCCCCCCCCCCCCCCCCCC
          FLORENCE_ADMIN_API_KEY: ci-admin-token-that-is-never-used
      - run: pnpm check
      - run: docker build --tag florence:${{ github.sha }} .
